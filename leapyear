#include <stdio.h>

int main()
{
    int a;
    printf("enter a number");
    scanf("%d",&a);
    
    if (a%4==0)
    printf("this year is leap");
    else
    printf("this year is not leap");
    
    return 0;
}
